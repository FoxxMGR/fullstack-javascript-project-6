extends ../layouts/application.pug

block header
  = t('views.labels.new.signUp')

block content
  +formFor()(action=route('updateLabel', { id: label.id }) method='post')
    +input(label, 'name', errors)(type="text")
    +submit(label)(value=t('views.labels.edit'))
