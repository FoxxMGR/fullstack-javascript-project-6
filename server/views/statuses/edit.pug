extends ../layouts/application.pug

block header
  = t('views.statuses.editHeader')

block content
  +formFor()(action=route('updateStatus', { id: status.id }) method='post')
    +input(status, 'name', errors)(type="text")
    +submit(status)(value=t('views.statuses.editSubmit'))
